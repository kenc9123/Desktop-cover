# 2026-06-30 Development Log

## Intent

Start the first runnable TopoShell prototype after the planning pass.

The user confirmed that I should proceed autonomously and added a documentation convention:

- Development logs must exist in both English and Chinese.
- README must exist in both English and Chinese.
- Chinese file names should add the `CN` suffix.

## Today's Scope

- Keep all changes inside `F:\Workspace\Personal\TopoShell`.
- Create bilingual development logs and README files.
- Build a phase 0 desktop prototype.
- Preserve the product direction: industrial functional style, minimal but not empty, simple without losing structured complexity.
- Avoid risky system-level behavior for now: no Explorer replacement, no taskbar hook, no registry shell changes.

## Implementation Direction

The first prototype should favor a native-feeling Windows desktop layer over a web-like application.

If WebView2 or external packages are inconvenient in the local environment, the phase 0 prototype may use pure WPF rendering first:

- Borderless dark desktop-like window.
- Low-brightness grid and terrain-inspired layers.
- Mouse-driven parallax.
- Static Dock prototype.
- Top-right system/status cluster.
- Command panel placeholder.

This keeps the prototype runnable without depending on network package restore.

## Notes

The visual target remains inspired by `sonic-topography`, but TopoShell is not a fork. `sonic-topography` is a music visualizer; TopoShell is a Windows desktop interaction layer.

## Completed

- Created bilingual documentation convention:
  - `README.md` for English.
  - `READMECN.md` for Chinese.
  - `dev-log/2026-06-30.md` for English.
  - `dev-log/2026-06-30CN.md` for Chinese.
- Created a .NET 8 WPF project at `src/TopoShell.App`.
- Implemented the phase 0 native prototype:
  - Borderless dark WPF window.
  - Industrial functional panel layout.
  - Low-brightness grid layer.
  - Terrain-inspired animated line field.
  - Mouse-driven parallax across scene, side rail, status panel, and Dock.
  - Bottom Dock prototype with basic launch buttons.
  - Command layer placeholder toggled by `Ctrl+Space` or the Dock `CMD` button.
  - Live system clock display.
- Added `.gitignore` entries for build output and local .NET cache folders.

## Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj --no-restore`
  - Result: success, 0 warnings, 0 errors.
- Short launch smoke test:
  - Started the built `TopoShell.App.exe`.
  - Confirmed the process stayed alive.
  - Closed it after the smoke test.

## Environment Note

The sandbox blocked normal .NET first-run writes to the user profile. To keep the project aligned with the workspace rule, the .NET CLI home and NuGet package cache were redirected into `F:\Workspace\Personal\TopoShell`.

## Direction Update

The user liked the motion quality of the first prototype, but clarified that the core concept should not center on a background field or app shortcuts.

New target:

- A circular or near-circular central device core.
- The core should turn toward different directions as the mouse moves.
- The circular panel can display system information such as CPU, GPU, memory usage, and time.
- App shortcut variants are intentionally deferred. The future desktop system may ship several specialized versions with different shortcut sets for different needs.

Implementation decision:

- Replace the visual focus with a central system core.
- De-emphasize Dock/shortcuts for now.
- Use native CPU and memory telemetry first.
- Keep GPU as a visible module slot with a provider placeholder until a stable GPU telemetry path is added.

## Second Pass Completed

- Reworked `MainWindow.xaml` around a central circular system core.
- Removed the bottom app shortcut Dock from the visual focus.
- Added mouse-driven core behavior:
  - Core tilt through skew/rotation/translation transforms.
  - Direction needle rotation.
  - Direction dot orbiting around the circular dial.
- Added live telemetry module:
  - CPU usage via `GetSystemTimes`.
  - Memory usage via `GlobalMemoryStatusEx`.
  - GPU slot reserved and clearly marked as not yet backed by a provider.
- Updated bilingual README files to reflect the new central-core direction.

## Second Pass Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj --no-restore`
  - Result: success, 0 warnings, 0 errors.
- Short launch smoke test:
  - Started the rebuilt `TopoShell.App.exe`.
  - Confirmed the process stayed alive.
  - Closed it after the smoke test.

## Third Direction Update

The user clarified that the second-pass circular panel still felt like a flat HUD skin rather than a real 3D device core.

New direction:

- The central core needs real depth and perspective, not just 2D rings.
- Use a black-and-white industrial UI language.
- Move closer to an Endfield-like functional sci-fi interface language while avoiding direct reuse of proprietary assets.
- Keep shortcuts deferred.
- Prioritize a layered mechanical core, strong silhouette, monochrome materials, and data readouts attached to the core.

Implementation plan:

- Replace the 2D center dial with a WPF `Viewport3D` model.
- Generate extruded rings and plates in code.
- Rotate the 3D assembly based on mouse position.
- Convert accent colors to black/white/gray.

## Third Pass Completed

- Replaced the flat circular HUD with a real WPF `Viewport3D` mechanical core.
- Generated 3D geometry in code:
  - Extruded outer rings.
  - Inner ring layers.
  - Raised center disk.
  - Radial mechanical plates.
  - Tick blocks around the outer ring.
- Added perspective mouse-driven rotation using 3D transforms.
- Converted the UI language to monochrome black/white/gray.
- Kept hardware telemetry in the central information layer.
- Added a process-level startup guard for environments where `windir` is missing, preventing WPF font cache startup crashes.

## Third Pass Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj --no-restore`
  - Result: success, 0 warnings, 0 errors.
- Short launch smoke test:
  - Started the rebuilt `TopoShell.App.exe`.
  - Confirmed the process stayed alive.
  - Closed it after the smoke test.

## Fourth Direction Update

The user clarified that telemetry must be physically attached to the 3D disk instead of floating as an independent 2D overlay.

New target:

- Time, CPU, GPU, memory, and audio modules should ride on the same 3D attitude transform as the disk.
- Add system audio listening.
- When music is playing, show current media information and album artwork if available.
- Album artwork should appear as a more dramatic animated module on the core, not a plain image box.

Implementation plan:

- Use `Viewport2DVisual3D` panels inside the same `Viewport3D`.
- Share the mechanical core transform with these panels so they move with the disk.
- Add native Core Audio peak metering.
- Add a media session monitor for current title/artist/album art where Windows exposes it.
- Animate the album module with pulse/rotation driven by audio level.

## Fourth Pass Completed

- Collapsed the old center-screen 2D telemetry overlay.
- Added `Viewport2DVisual3D` panels for time, telemetry, audio peak, and media artwork.
- Attached those panels to the same 3D transform used by the mechanical disk, so they pitch, yaw, and roll with the core instead of floating above it.
- Added native Core Audio output peak metering through the default render endpoint.
- Added Windows global media session metadata reading:
  - title
  - artist
  - album
  - thumbnail artwork when the active player exposes it
- Added an album reactor module:
  - rotating monochrome ring system
  - pulsing cover frame driven by audio level
  - fallback live-audio / standby states when no artwork is exposed

## Fourth Pass Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
- Short launch smoke test:
  - Started the rebuilt `TopoShell.App.exe`.
  - Confirmed the process stayed alive.
  - Closed it after the smoke test.

## Fifth Direction Update

The user reviewed screenshots and called out three issues:

- The previous center telemetry layer was only hidden, not removed.
- The disk tilt was too aggressive; moving the mouse toward the bottom made the disk nearly side-on, hiding the attached information.
- The media artwork reactor rotated too much and looked comedic instead of functional.

New correction plan:

- Delete the old 2D center telemetry layer instead of keeping it collapsed.
- Keep information as flat decals close to the disk surface rather than tall floating plates.
- Reduce the mouse-driven pitch range so the top surface remains readable.
- Convert the radial mechanical bars into audio-reactive physical bars.
- Change the media artwork module into a stable cover bay with subtle pulse only.

## Fifth Pass Completed

- Removed the old collapsed center telemetry XAML layer completely.
- Removed the matching old telemetry control writes from `MainWindow.xaml.cs`.
- Reduced mouse-driven attitude range:
  - Smaller pitch travel so the disk does not become an unreadable side profile.
  - Smaller parallax translation for the core stage.
  - Slightly higher camera position for better top-surface readability.
- Replaced the previous large floating panels with compact surface decals for:
  - media bay
  - telemetry
  - time
- Converted the radial mechanical bars into audio-reactive 3D bars driven by the Core Audio output peak.
- Changed the album artwork module from a rotating reactor into a stable cover bay with subtle audio-level pulse.
- Added `.qa/` to `.gitignore` for project-local screenshot verification artifacts.

## Fifth Pass Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
- Short launch and screenshot QA:
  - Started the rebuilt `TopoShell.App.exe`.
  - Captured a project-local QA screenshot at `.qa/toposhell-fifth-pass.png`.
  - Captured a bottom-mouse attitude screenshot at `.qa/toposhell-fifth-pass-bottom.png`.
  - Confirmed the old center overlay is gone.
  - Confirmed surface decals remain visible in the default attitude.
  - Confirmed the bottom-mouse attitude no longer collapses the disk into an unreadable side profile.
  - Closed the app after the smoke test.

## Sixth Direction Update

The user reviewed the fifth-pass screenshot and clarified that the disk is still too steep for readable surface information.

New correction plan:

- Move the camera toward a calmer top-down view.
- Keep only a slight disk attitude angle for depth.
- Reduce mouse-driven pitch, yaw, and roll again.
- Preserve the surface-decal approach and audio-reactive radial bars.

## Sixth Pass Completed

- Changed the core camera to a more top-down composition:
  - Higher camera position.
  - Shorter forward distance.
  - Wider final field of view so the disk fits the stage.
- Reduced the default disk pitch from a steep display angle to a slight attitude angle.
- Reduced mouse-driven pitch, yaw, roll, and stage translation again to prioritize readability.
- Kept media, telemetry, and time as disk-surface decals.
- Kept the radial 3D bars as the audio-reactive visualization.

## Sixth Pass Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
- Short launch and screenshot QA:
  - Captured `.qa/toposhell-sixth-pass-flat.png`.
  - Captured `.qa/toposhell-sixth-pass-flat-final.png` after widening the field of view.
  - Confirmed the disk is much flatter and the surface text is more readable.
