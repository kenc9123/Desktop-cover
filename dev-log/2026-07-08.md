# 2026-07-08 Development Log

## Goal

Respond to full-screen feedback: the center mechanical disk feels too small, and the disk-surface information is too small to read comfortably.

## Scope

- Keep all changes inside `F:\Workspace\Personal\TopoShell`.
- Preserve the existing WPF 3D core structure.
- Improve full-screen scale and readability without replacing the disk UI concept.

## Changes

- Wrapped the fixed 660x620 core stage in a bounded `Viewbox` so the center disk scales up with available full-screen space while still shrinking safely on smaller windows.
- Slightly tightened the 3D camera field of view and camera distance so the physical disk fills more of the viewport.
- Enlarged the disk-attached media, telemetry, and time decals:
  - larger physical panel meshes
  - larger panel visual sizes
  - larger clock, metric, media, and label text
  - larger telemetry bars and album-art bay
- Increased panel opacity and border contrast a little for better readability on the moving disk surface.
- Enabled display text formatting and ClearType rendering on generated disk text blocks.

## Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
- `git diff --check`
  - Result: no whitespace errors.

## Notes

- The first sandboxed build could not write WPF generated files under the project build folders, so the final build was rerun with approval while keeping `DOTNET_CLI_HOME` and `NUGET_PACKAGES` inside the project.
- I did not launch the full-screen shell UI for a screenshot in this pass to avoid taking over the desktop during the coding turn.

## Bottom Dock Pass

The user shared a full-screen screenshot showing the enlarged disk covering too much of the lower area, then asked for the lower dock bar to support custom common software shortcuts.

Changes:

- Reduced the core stage maximum size from the previous full-screen scale and aligned it toward the top of the center lane so the bottom bar stays clear.
- Increased the bottom row height and replaced the placeholder footer with an actual Dock strip.
- Added `dock-shortcuts.json` as the editable shortcut profile copied beside the app.
- Added Dock loading with a safe fallback list if the JSON is missing or malformed.
- Added Dock buttons that show extracted executable icons when possible, fall back to a text initial, and launch via `ProcessStartInfo` with shell execution.
- Kept the command slot available as a smaller `CTRL+SPACE` button on the right side of the Dock.

Verification:

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.

## README And GitHub Publish Prep

The user asked to push the current work to GitHub and reminded me to update the README first.

Changes:

- Updated `README.md` to describe the configurable Dock, dock profile format, and current full-screen layout behavior.
- Rebuilt `READMECN.md` as a clean Chinese README because the previous file content was not rendering readably in the local terminal.
- Added the July 8 development log links to both READMEs.
- Confirmed the local remote is `https://github.com/kenc9123/Desktop-cover.git`.

Verification:

- `git diff --check`
  - Result: no whitespace errors.
