# 2026-07-03 Development Log

## Goal

Refine the mouse parallax motion based on the user's Arknights: Endfield reference video.

The user specifically called out that the current dynamic effect does not match the desired reference and asked to copy the mouse parallax feel from the supplied local video.

## Scope

- Keep all project changes inside `F:\Workspace\Personal\TopoShell`.
- Do not alter the current disk UI structure unless needed for motion.
- Focus on motion behavior:
  - smoother target following
  - delayed inertial response
  - layered parallax depths
  - subtle 3D attitude change
  - less abrupt direct mapping from mouse position to UI transforms

## Implementation Plan

- Replace immediate mouse-to-transform updates with target and current parallax state.
- Smooth the current parallax toward the target every render frame.
- Add a small velocity component so motion feels like it has weight.
- Drive background grid, terrain, side rails, and core stage from different parallax depths.
- Keep the disk readable while adding subtle Endfield-like depth motion.

## Completed

- Added `MouseLeave` handling so the parallax target returns to center when the pointer leaves the window.
- Replaced direct `MouseMove` transform writes with:
  - `_parallaxTarget`
  - smoothed `_parallax`
  - `_parallaxVelocity`
- Added spring-damper motion in the render loop.
- Added `SmoothStep` shaping so mouse movement eases in and out rather than mapping linearly.
- Split layer depth:
  - fine grid moves as a far background layer
  - terrain line field moves as a nearer background layer
  - side rails move as foreground UI plates
  - the core stage moves as the main focal layer
- Added velocity influence to the disk's pitch, yaw, and roll so it has a slight weighted drag without losing readability.

## Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
- Short launch and mouse-movement smoke test:
  - Started `TopoShell.App.exe`.
  - Moved the pointer across several window regions.
  - Captured `.qa/toposhell-20260703-parallax-smoke.png`.
  - Confirmed the app stayed alive and the disk remained readable.

## Seventh Direction Update

The user clarified that horizontal mouse movement should make the disk tilt left and right, not simply sway or rotate sideways.

Correction plan:

- Reduce X-driven yaw and stage translation.
- Make X-driven roll the primary response.
- Keep the spring-damper parallax model from the previous pass.
- Preserve readability by keeping tilt subtle.

## Seventh Pass Completed

- Reduced horizontal mouse influence on disk yaw from the primary response to a subtle secondary response.
- Increased horizontal mouse influence on disk roll so left/right movement produces a visible side tilt.
- Reduced horizontal translation of the core stage so it no longer reads as simple left/right sway.
- Kept vertical pitch and layered parallax behavior unchanged.

## Seventh Pass Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
- Short launch and side-movement smoke test:
  - Started `TopoShell.App.exe`.
  - Moved the pointer to the left side, right side, and center.
  - Captured `.qa/toposhell-20260703-side-tilt-smoke.png`.
  - Confirmed the app stayed alive.

## Eighth Direction Update

The user asked what else could be improved and gave permission to continue polishing.

This pass should make TopoShell feel more like a usable desktop layer instead of only a visual prototype. The safest next step is a reversible runtime control layer:

- Add a tray entry so the shell can be hidden and restored without killing the process.
- Add runtime toggles for motion, audio response, and always-on-top behavior.
- Turn the command slot into a small control console with real actions.
- Keep shortcut/application profiles deferred, matching the earlier product direction.

## Eighth Implementation Plan

- Enable the Windows Forms tray icon support used by `NotifyIcon`.
- Add a tray menu with show/hide, motion, audio, topmost, reset view, and exit commands.
- Route the title-bar minimize action to hide-to-tray.
- Replace the placeholder command slot with compact action buttons.
- Make motion and audio toggles affect the live render loop immediately.

## Eighth Pass Completed

- Enabled Windows Forms support in the WPF project for `NotifyIcon`.
- Added a system tray icon with:
  - show/hide
  - motion toggle
  - audio response toggle
  - always-on-top toggle
  - reset view
  - exit shell
- Changed the title-bar minimize action to hide the shell to the tray.
- Replaced the command-slot placeholder with runtime control buttons.
- Added a clickable footer command slot in addition to the `Ctrl+Space` keyboard path.
- Added immediate render-loop behavior for the motion and audio toggles.
- Kept shortcut/application profile work deferred.

## Eighth Pass Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
- Short launch and UI smoke test:
  - Started `TopoShell.App.exe`.
  - Opened the command slot from the footer.
  - Captured `.qa/toposhell-20260703-command-slot-click.png`.
  - Confirmed the command controls render without visible overflow.

## Ninth Direction Update

The user asked to try replacing `Explorer.exe`.

This is now outside the original Phase 0 safety boundary, so the next step is not to change the live system immediately. The project will add a reversible, current-user shell replacement kit inside the repository:

- Do not replace the `explorer.exe` file on disk.
- Do not write HKLM machine-wide shell settings.
- Prepare HKCU scripts for enabling and disabling TopoShell as the current user's shell.
- Store the original shell value in a project-local backup file before enabling shell mode.
- Provide an explicit recovery path through Task Manager / PowerShell.

## Ninth Implementation Plan

- Add `tools/shell-mode` scripts:
  - check current shell setting
  - enable TopoShell as current-user shell
  - restore Explorer shell
  - start Explorer fallback manually
- Add English and Chinese shell-mode documents.
- Update README safety wording to distinguish normal mode from the opt-in shell experiment.

## Ninth Pass Completed

- Added `tools/shell-mode/check-shell-mode.ps1`.
- Added `tools/shell-mode/enable-toposhell-shell.ps1`.
- Added `tools/shell-mode/restore-explorer-shell.ps1`.
- Added `tools/shell-mode/start-explorer-fallback.ps1`.
- Added English documentation: `docs/04-shell-replacement-experiment.md`.
- Added Chinese documentation: `docs/04-shell-replacement-experimentCN.md`.
- Updated README files to document the opt-in shell experiment.
- Did not enable shell replacement on the live system.

## Ninth Pass Verification

- Ran the read-only check script:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools/shell-mode/check-shell-mode.ps1`
  - Result: current user shell resolves to `explorer.exe`.
  - Result: no project backup exists yet.
- Fixed script handling for accounts where the HKCU `Shell` value does not exist.
