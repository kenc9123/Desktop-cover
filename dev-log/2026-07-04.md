# 2026-07-04 Development Log

## Goal

Publish TopoShell as a new GitHub repository.

## Scope

- Keep all project file changes inside `F:\Workspace\Personal\TopoShell`.
- Prepare a clean local git repository for TopoShell.
- Commit the current project state before pushing.
- Prefer a private GitHub repository named `TopoShell` unless the user specifies otherwise.

## Current Constraint

- The local `gh` CLI is not installed.
- The available GitHub connector tools can work with existing repositories, files, branches, issues, and pull requests, but do not expose a create-repository operation in this session.

## Plan

- Add this development log before repository work.
- Initialize a standalone git repository in `TopoShell`.
- Verify ignore rules exclude local caches, build output, and QA screenshots.
- Create an initial commit.
- Push once a GitHub remote exists or a repository creation tool becomes available.

## Local Repository Preparation

- Initialized a standalone git repository in `F:\Workspace\Personal\TopoShell`.
- Confirmed the branch is `main`.
- Added `tools/shell-mode/shell-backup.json` to `.gitignore` because it is machine-local recovery state.
- Confirmed ignored paths include:
  - `.qa/`
  - `dotnet-home/`
  - `nuget-packages/`
  - `src/**/bin/`
  - `src/**/obj/`

## Verification

- `dotnet build src/TopoShell.App/TopoShell.App.csproj`
  - Result: success, 0 warnings, 0 errors.
